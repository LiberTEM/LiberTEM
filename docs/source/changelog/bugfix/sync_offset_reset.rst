[Bugfix] Correctly reset sync_offset field in web interface
===========================================================

* The dataset load Reset button now correctly resets the sync_offset
  field in the web UI, if present (:pr:`1378`).
