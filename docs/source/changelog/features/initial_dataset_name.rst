[Feature] Initially set file's basename as dataset name in GUI
===============================================================

* When opening a file with the GUI for the first time, dataset name is initially set to the file's basename (:pr:`642`)
