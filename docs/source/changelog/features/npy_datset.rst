[Features] Add support for .npy files as datasets
=================================================

* Add the :class:`~libertem.io.datasets.npy.NPYDataSet` class which
  supports interpreting `.npy` binary files as a LiberTEM dataset.
