[Feature] Add an optional progress bar
======================================

* Can be enabled by passing :code:`progress=True` to the
  :meth:`~libertem.api.Context.run_udf` method (:pr:`613`).
* Add a dependency on tqdm
