[Misc] Move to `hatchling` for packaging + build
================================================

* LiberTEM has been migrated to use `hatchling` as
  its build backend and all metadata moved into
  :code:`pyproject.toml`. (:pr:`1667`)