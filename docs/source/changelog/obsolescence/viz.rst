Import colormaps from empyre
============================

* Remove color wheel code from viz.py and replace with imports from empyre.
  Note that these functions expect three vector components instead of two. (:pr:`851`)